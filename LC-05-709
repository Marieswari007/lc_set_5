char* toLowerCase(char* s) {
    char* c=s;
   int len=strlen(s);
    for(int i=0;i<len;i++){
        if(s[i]>='A' && s[i]<='Z'){
            int c=s[i];
            s[i]=c+32;
            
        }
    }
    return s;
    
}
